# オトミエールボヤー MVP仕様書

## 1. 概要

**オトミエールボヤー（OtoMieru Boyer）** は、Linuxネイティブで動作する、耳コピ用途に特化した軽量音声プレイヤーである。

目的は音響解析そのものではなく、**「音をぼやーっと視覚化しながら、耳コピを楽にする」**こと。

Sonic Visualiserのような総合音響解析ツールではなく、WaveToneに近い「音楽を聴いて採譜する人」の作業フローを優先する。

基本思想は以下。

* 音響工学の知識を要求しない
* 設定項目を極力見せない
* 起動してすぐ耳コピできる
* ピアノロールとスペクトログラムを一体化する
* 再生操作と視覚表示が常に同期する
* MuseScore等との連携はMVP外とする

---

## 2. 想定ワークフロー

現在の利用フローは以下。

```text
YouTube等で曲を探す
        ↓
既存の自作Rustダウンロードアプリ
        ↓
音声ファイル
        ↓
オトミエールボヤー
        ↓
耳コピ
        ↓
MuseScoreへ手入力
```

ダウンロード機能はオトミエールボヤーには統合しない。

理由は責務を明確に保つため。

```text
DLアプリ
責務：音源を取得する

オトミエールボヤー
責務：音源を聴いて音を見つける

MuseScore
責務：楽譜を書く
```

将来的にはCLI・ファイル関連付け・MIDI等による連携を検討するが、MVPでは独立アプリとする。

---

## 3. MVPの目的

MVPでは以下の作業を快適に行えることを完成条件とする。

```text
曲を開く
 ↓
再生する
 ↓
聞き取りにくければ速度を落とす
 ↓
聞きたい部分を選択してループ
 ↓
スペクトログラムを見る
 ↓
ピアノ鍵盤上の位置から音程を推測
 ↓
スペクトログラムをクリック
 ↓
対応する基準音を鳴らして確認
 ↓
MuseScoreへ記譜
```

---

# 4. MVP機能

## 4.1 音声ファイル読み込み

ローカルの音声ファイルを読み込めること。

初期対応候補：

* WAV
* MP3
* FLAC
* OGG

内部デコードには **Symphonia** を使用する。

SymphoniaはRust製の音声デコード／デマルチプレクスライブラリで、WAV、MP3、FLAC、OGG/Vorbis、AACなど多数の形式を扱える。

ドラッグ＆ドロップ対応はMVP後半、またはMVP直後でもよい。

最低限、

```text
Open
↓
ファイル選択
↓
読み込み
```

が動けばよい。

---

## 4.2 音声再生

以下を実装する。

* 再生
* 一時停止
* 再開
* 停止
* シーク
* 現在位置表示
* 曲全体時間表示

音声出力には **CPAL** を使用する。

CPALはRustAudioの低レベルなクロスプラットフォーム音声I/Oライブラリであり、Linux上でのネイティブ音声出力に利用できる。

---

## 4.3 スペクトログラム表示

音声ファイル読み込み時にSTFTを行い、時間×周波数のスペクトログラムデータを生成する。

解析は基本的に**事前計算方式**とする。

```text
音声ファイル
 ↓
PCMへデコード
 ↓
STFT
 ↓
スペクトログラムデータ
 ↓
表示用キャッシュ
```

再生中にFFTを繰り返し計算する方式にはしない。

理由：

* 再生処理と解析処理を分離できる
* 描画が安定する
* シークしやすい
* 速度変更しても解析データを作り直さなくてよい
* 同じ音源を繰り返し再生しても解析負荷が発生しない

FFTには **RustFFT** を使用する。

RustFFTはRust製のFFTライブラリで、FFT計算そのものを担当させる。

---

## 4.4 ピアノロール表示

スペクトログラムの縦軸は単純なHz表示ではなく、**音楽的な音階位置**に変換する。

左側にピアノ鍵盤を表示する。

概念：

```text
C6 │
B5 │       ░▒▓██
A#5│   ░▒██████
A5 │
G#5│            ▒▓██
G5 │
...
C4 │ █████████
```

ピアノ鍵盤とスペクトログラムの高さを完全に一致させる。

音程変換にはMIDIノート番号相当の内部座標を使用する。

基準：

```text
A4 = MIDI 69 = 440 Hz
C4 = MIDI 60 ≒ 261.63 Hz
```

画面上では「Hzを解析している」という感覚をユーザーに与えない。

あくまで、

**どの鍵盤の高さに音が現れているか**

を見るUIとする。

---

## 4.5 音の強さを色・濃淡で表示

各時間・音程位置におけるエネルギー量を視覚化する。

音が弱い箇所：

```text
░
```

強い箇所：

```text
▒▓██
```

のように濃くなる。

ユーザー向けUIでは以下のような項目は原則表示しない。

* FFT size
* Window function
* dB scale
* Overlap
* Bin width
* Spectral threshold

これらは内部固定値または将来の「詳細設定」に隠す。

**工学ツール化しないことを優先する。**

---

## 4.6 再生位置同期スクロール

再生位置とスペクトログラムを常に同期させる。

基本UIは、

```text
過去 ←             現在             → 未来

████▒░        ▒███ │ ░██
    ▒██░           │
████████████        │
                    ↑
                  再生位置
```

とする。

再生カーソルを一定位置に固定し、スペクトログラム側をスクロールさせる方式を基本とする。

停止・一時停止時には画面も停止する。

シークした場合は即座にその時間位置へ移動する。

---

## 4.7 スペクトログラムクリックによる音程確認

スペクトログラム上をクリックすると、そのY座標に対応した音程を鳴らす。

例：

```text
クリック
 ↓
Y座標
 ↓
MIDI note 68
 ↓
G#4
 ↓
基準音を短く再生
```

同時に画面上へ、

```text
G#4
415.30 Hz
```

程度を表示してもよい。

ただし主目的は**耳で確認すること**なので、数値情報を前面には出さない。

初期実装では正弦波でもよい。

将来的にはピアノ系のSoundFont等へ変更可能とする。

---

## 4.8 ピッチを維持した再生速度変更

音程を変えずに再生速度を変更できる。

最低限：

```text
0.50x
0.75x
1.00x
```

を提供する。

可能ならスライダーまたは追加段階として、

```text
0.50
0.60
0.70
0.75
0.80
0.90
1.00
```

なども選択可能にする。

単純なサンプルレート変更ではなく、**タイムストレッチ**を使用する。

候補は **Rubber Band Library**。

Rubber Bandは音声のテンポとピッチを独立して変更できる、タイムストレッチ／ピッチシフト用ライブラリである。

Rubber BandはC++ライブラリなので、Rust側からFFIまたは既存バインディングを利用する構成を想定する。

ここは本プロジェクトで最も外部依存が強い部分とする。

---

## 4.9 範囲選択

スペクトログラム上を横方向にドラッグして時間範囲を選択できる。

```text
      selected
     <-------->
─────██████████──────────
```

選択範囲は半透明オーバーレイ等で明示する。

開始時刻と終了時刻を内部保持する。

例：

```text
start = 42.15 sec
end   = 44.80 sec
```

---

## 4.10 選択範囲ループ再生

選択範囲についてLoopをONにすると、その区間のみ繰り返し再生する。

```text
42.15 sec
   ↓
再生
   ↓
44.80 sec
   ↓
42.15 secへ戻る
```

耳コピ用途では主要機能なのでMVP必須。

選択範囲を解除した場合は通常再生へ戻る。

---

# 5. UI仕様

## 5.1 基本方針

**画面を見ただけで使い方がわかること。**

Sonic Visualiserのように「解析レイヤー」「解析方式」「プラグイン」等を理解する必要のあるUIにはしない。

基本画面：

```text
┌─────────────────────────────────────────────┐
│ Open   ▶ / ⏸       0.75x      01:24 / 04:02 │
├────────┬────────────────────────────────────┤
│        │                                    │
│ Piano  │          Spectrogram               │
│        │                │                   │
│        │                │ current           │
│        │                                    │
│        │     [ selected loop ]              │
│        │                                    │
├────────┴────────────────────────────────────┤
│ Loop: ON                                    │
└─────────────────────────────────────────────┘
```

MVPではツールバーすら最小化する。

---

# 6. 操作仕様

基本操作：

| 操作           | 動作        |
| ------------ | --------- |
| Space        | 再生 / 一時停止 |
| スペクトログラムクリック | 音程確認      |
| 横ドラッグ        | ループ範囲選択   |
| タイムラインクリック   | シーク       |
| Loop ON      | 選択範囲反復    |
| Speed        | 再生速度変更    |

将来的には、

```text
← / →
数秒戻る・進む

Shift + Click
MuseScoreへMIDI送信
```

等を追加可能。

---

# 7. 技術選定

## 7.1 言語

**Rust**

を採用する。

理由：

* Linuxネイティブアプリとして配布しやすい
* 既存の自作DLアプリと同じ言語で統一できる
* 音声処理との相性がよい
* CPU負荷の高いFFTや描画にも十分対応できる
* Pythonランタイム等をユーザーへ要求しない
* 将来CLI連携もしやすい
* バイナリ単体へ近い形で配布しやすい

Pythonによる試作は行わず、最初からRustで実装する。

---

## 7.2 GUI

**egui / eframe**

を第一候補とする。

eguiはRust製のImmediate Mode GUIで、ネイティブアプリとして利用できる。

今回のUIは通常の業務フォームより、

* スペクトログラム
* ピアノ鍵盤
* 再生カーソル
* 範囲選択
* 独自マウス操作

といったカスタム描画が中心であるため、Immediate Mode GUIとの相性が良い。

Qtほど大規模なGUI基盤はMVPでは不要と判断する。

---

# 8. 推奨内部構成

以下のように責務を分割する。

```text
src/
├── main.rs
│
├── app/
│   ├── mod.rs
│   └── state.rs
│
├── audio/
│   ├── mod.rs
│   ├── decoder.rs
│   ├── player.rs
│   ├── timestretch.rs
│   └── preview_tone.rs
│
├── analysis/
│   ├── mod.rs
│   ├── stft.rs
│   ├── spectrum.rs
│   └── pitch_map.rs
│
├── ui/
│   ├── mod.rs
│   ├── toolbar.rs
│   ├── piano.rs
│   ├── spectrogram.rs
│   └── timeline.rs
│
└── model/
    ├── mod.rs
    ├── track.rs
    ├── playback.rs
    └── selection.rs
```

---

# 9. 各モジュール責務

## audio::decoder

担当：

* 音声ファイル読み込み
* Symphoniaによるデコード
* PCMへの変換
* サンプルレート取得
* チャンネル情報取得

UIについては一切知らない。

---

## audio::player

担当：

* PCM再生
* 再生開始
* 一時停止
* 停止
* シーク
* 現在再生位置管理
* ループ境界処理

CPALを使用する。

---

## audio::timestretch

担当：

```text
PCM
 ↓
Rubber Band
 ↓
tempo変更済PCM
```

ピッチ維持速度変更のみを担当する。

解析処理とは完全に分離する。

---

## audio::preview_tone

スペクトログラムクリック時に基準音を鳴らす。

入力：

```text
MIDI note
```

または

```text
frequency
```

出力：

短い確認音。

---

## analysis::stft

担当：

* Windowing
* FFT
* Frame生成
* Magnitude計算

ユーザーUIには解析パラメータを公開しない。

---

## analysis::spectrum

STFT結果を表示可能なデータ構造へ変換する。

例：

```text
time
pitch
intensity
```

の2次元データ。

---

## analysis::pitch_map

担当：

```text
frequency ↔ MIDI note
screen Y ↔ pitch
```

変換。

音楽座標と解析座標の橋渡し部分。

---

## ui::spectrogram

アプリの中心。

担当：

* スペクトログラム描画
* 現在位置描画
* 時間スクロール
* マウスクリック
* 範囲選択ドラッグ
* 強度表示

音声再生処理そのものは行わない。

---

## ui::piano

担当：

* 鍵盤表示
* 音名表示
* スペクトログラムとの縦位置同期

---

## ui::toolbar

担当：

```text
Open
Play/Pause
Speed
Time
Loop
```

のみ。

---

# 10. 中心データモデル

概念的には、

```rust
Track {
    samples,
    sample_rate,
    duration,
    spectrogram,
}

PlaybackState {
    position,
    playing,
    speed,
    loop_enabled,
}

Selection {
    start,
    end,
}
```

程度から開始する。

UIコンポーネントが個別に音声状態を持つ構成にはしない。

---

# 11. スペクトログラムと再生の関係

ここは設計上重要。

スペクトログラムは**元音源の時間座標**を保持する。

速度を0.5xにしても解析データそのものは変更しない。

```text
Original Audio Timeline
0────10────20────30 sec
        ↑
        position
```

0.5x再生時には、

**positionが進む速度だけを遅くする。**

これにより、

* 表示データを再生成しない
* 速度変更直後も画面が崩れない
* シーク座標が常に元音源時間になる
* ループ範囲も元音源時間で管理できる

というメリットがある。

---

# 12. スレッド構成

最低限、

```text
UI Thread
  │
  ├─ input
  ├─ drawing
  └─ state update

Audio Thread
  │
  └─ playback callback

Worker
  │
  └─ decode / STFT
```

程度に分ける。

解析中にGUIが固まらないこと。

音声コールバック側では重い処理を行わない。

---

# 13. MVP外

以下は意図的に実装しない。

* YouTubeダウンロード
* MuseScore直接操作
* MIDI出力
* 自動採譜
* コード認識
* BPM解析
* 楽器分離
* ボーカル除去
* AI解析
* 波形編集
* 音声編集
* イコライザ
* VST
* プラグインシステム
* 詳細FFT設定
* 複数トラック
* DAW機能

「できるから追加する」は禁止。

---

# 14. 将来候補

MVP完成後、実際に使用して必要性が確認されたもののみ追加する。

候補：

### DLアプリ連携

```text
DL完了
 ↓
otomieru-boyer song.mp3
```

CLI引数によるファイルオープン。

---

### Linuxファイル関連付け

MP3等を、

```text
Open with OtoMieru Boyer
```

で開く。

---

### MuseScore連携

候補：

* 仮想MIDI
* MIDI Note送信
* Shift+Clickでノート入力
* 独自プラグイン連携

ただし別フェーズ。

---

### クリック音の改善

初期：

```text
sine wave
```

将来：

```text
piano sample
SoundFont
```

---

# 15. 開発順序

実装順は以下を推奨する。

1. Rust + eframe/eguiで空ウィンドウ起動
2. WAV/MP3読み込み
3. 通常速度で音声再生
4. 再生 / 一時停止 / シーク
5. STFT計算
6. スペクトログラム静止表示
7. ピアノ鍵盤との縦軸一致
8. 再生位置カーソル
9. 再生同期スクロール
10. スペクトログラムクリック → 基準音
11. ドラッグ範囲選択
12. 範囲ループ
13. Rubber Bandによるピッチ維持速度変更
14. UI整理
15. 自分の実際の耳コピ作業で使用

特に**速度変更は最初に実装しない**。

先に、

```text
再生
+
スペクトログラム
+
同期
```

というアプリの中心を完成させる。

---

# 16. MVP完成条件

以下が一つのアプリで問題なく行えればMVP完成とする。

> MP3を開く。
> 再生する。
> スペクトログラムがピアノ鍵盤と同期して流れる。
> 0.5xまたは0.75xへ落としても音程が変わらない。
> 聞き取りたい部分をドラッグしてループできる。
> スペクトログラム上の音をクリックすると対応する基準音が鳴る。
> その結果を見ながらMuseScoreへ耳コピできる。

---

# 17. 技術スタックまとめ

```text
Language
└─ Rust

GUI
└─ egui / eframe

Audio Decode
└─ Symphonia

Audio Output
└─ CPAL

FFT / STFT
└─ RustFFT

Time Stretch
└─ Rubber Band Library

Application
└─ Linux native desktop app
```

eguiはネイティブ動作するRust GUI、Symphoniaは多数の音声形式を扱えるRust音声デコーダ、CPALは低レベル音声I/O、RustFFTはFFT計算、Rubber Bandはテンポとピッチを独立して変更するタイムストレッチ用途という分担にする。

---

## 最重要設計原則

**オトミエールボヤーは音響解析ソフトではない。**

内部では高度な音声解析を行っていても、ユーザーが見るものは、

```text
開く
聴く
遅くする
繰り返す
見る
クリックして確かめる
```

だけにする。

「音がぼやーっと見える。」

それだけで目的を達成できるアプリを目指す。
