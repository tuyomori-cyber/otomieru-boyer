# Otomieru Boyer 仕様書

## 概要

音声をスペクトログラムとピアノロールで可視化し、反復練習や聴音を支援するLinux向け再生アプリケーションである。再生速度を変えても音程を維持でき、独立したオクターブシフト、5バンドEQ、試聴音を備える。

## 再生機能

| 機能 | 仕様 |
| --- | --- |
| 再生制御 | 再生、一時停止、停止、曲頭へ移動 |
| シーク | 停止中にスペクトログラムをクリックして移動 |
| 曲頭へ移動 | 再生位置とスペクトログラムの表示範囲を0秒へ戻す |
| ループ | タイムラインのドラッグで範囲指定し、`Loop`で有効化 |
| 速度変更 | 0.50x / 0.75x / 1.00x / 1.25x / 1.50x。音程を維持する |
| オクターブ | -1 oct / 0 st / +1 oct。速度変更と独立して併用できる |

速度、オクターブ、EQは停止中に変更する。変更後はworkerが新しい設定で先読みしてから再生する。

## スペクトログラムと再生線

- 解析対象はMIDI 24〜108。
- ホイールで時間軸を、Ctrl＋ホイールで音高方向を拡大縮小する。どちらもポインタ位置を中心にズームする。
- 音高方向のズームでは、左のピアノロールも同じ音高範囲へ同期する。
- スペクトログラムを押下している間は、対応するMIDIノートを試聴し、ピアノロール上の同じ鍵をピンクで着色する。
- 表示領域はタイムライン・ステータス行を除いたウィンドウの残り高さへ追従する。
- 再生位置は音声スナップショット間をUI側で補間する。シーク、停止、巻き戻りでは補間をリセットして即時同期する。
- 再生線は小数の物理ピクセル位置を隣接2列への輝度分配で描画する。メイン表示と下部バーの両方に適用する。
- 時間方向の描画は表示幅の物理ピクセル数に合わせて解析フレームを集約する。1列に複数フレームが対応する場合は音高ごとの最大強度を用い、短いピークを保持する。
- スペクトログラム本体はテクスチャへキャッシュする。再生中はキャッシュ画像と再生線・ガイド類だけを描画し、解析データの全走査とセル矩形の生成は行わない。
- キャッシュは表示範囲、表示サイズ、音程範囲、表示ゲイン、基音強調、強調音階、指定外音の減衰、EQ、音源の切替時に再生成する。
- 再生中はUI FPS、フレーム時間、表示スケールを診断表示する。60 FPS前後での表示を目標とする。
- `Heat`は表示用の全体ゲインであり、再生音には影響しない。

## 調整・設定

上部の`調整・設定`ボタンは1つのダイアログを開く。EQと基音強調を左右に常時並べ、音色選択を下部に表示する。各項目を切り替えず、同時に確認・調整できる。

### イコライザ

次の5帯域を各±12 dBで設定する。

| バンド | 中心周波数 | フィルタ |
| --- | ---: | --- |
| Low | 100 Hz | Low shelf |
| Low-mid | 250 Hz | Peaking |
| Mid | 1 kHz | Peaking |
| High-mid | 4 kHz | Peaking |
| High | 10 kHz | High shelf |

EQはworker内で再生音へ適用する。スペクトログラムも同じEQカーブで明るさを補正し、強調帯域は明るく、減衰帯域は暗く表示する。

### 基音強調

基音候補とその2〜6倍音の強度を用い、選択したキー／スケールまたは個別指定した音を強調する。指定外の音は0〜100%で減衰率を調整できる。表示専用の補正であり、再生音とEQ設定には影響しない。

### 試聴音

- 音色はピアノ、シンセストリングス、矩形波から選択する。
- 試聴音量はツールバーで0〜50%の範囲で調整する。
- ピアノはFreePats Piano FB smallの23サンプルから最も近い音程を選び、リサンプリングして再生する。
- シンセストリングスはFreePats Synth Strings #1の12サンプルを使い、SFZ指定のループ範囲で持続再生する。
- 矩形波は録音サンプルとの音量差を抑えるため、試聴音量に25%の出力補正を掛ける。

## 音声処理アーキテクチャ

```text
UI / Transport
  → Streaming worker（Rubber Band R2、pitch、EQ）
  → 固定長SPSCリングバッファ
  → CPAL audio callback
```

- 音声コールバックはリング読み出しだけを行い、メモリ確保・Mutex・DSP計算をしない。
- workerは固定サイズの作業領域で処理し、音源全体の加工済みバッファを保持しない。
- Seek / Stop / LoopJumpではgenerationを更新し、旧世代の音声を破棄する。
- Rubber BandはリアルタイムR2 engineを使用する。
- 試聴音は別のCPAL出力ストリームで再生する。音色・音程・音量はアトミックな状態で更新し、出力コールバック内で反映する。

## 動作環境・ライセンス

- 対象環境はLinux。
- ビルド時に `librubberband-dev`、実行時に `librubberband.so` が必要。
- 日本語UIには Noto Sans CJK または IPAex を利用する。
- 本体と Rubber Band 利用部分は GPL-2.0-or-later 条件で公開する。
- FreePats Piano FB smallおよびSynth Strings #1をCC0 1.0素材として同梱する。サンプル、SFZ対応表、配布元ライセンス文はそれぞれ`assets/piano-fb`と`assets/synth-strings-1`に保持する。詳細は[外部依存関係と同梱音源](docs/dependencies.md)を参照する。
