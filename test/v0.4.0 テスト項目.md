# v0.4.0 テスト項目

## 目的

v0.4.0の比較ループとループ再生中編集を確認するとともに、既存の音高メモ、レイヤー、保存・読込、再生機能の回帰を確認する。

- `[OK]`: 確認済み・問題なし
- `[ ]`: 未確認
- `[NG]`: 確認済み・問題あり
- 実機確認日、OS、音声デバイス、ディスプレイ条件などは、必要に応じて各項目の末尾へ記録する。

## 1. 自動テスト

- [OK] `AUT-01` `cargo fmt --check`が成功する。
- [OK] `AUT-02` `cargo clippy --all-targets -- -D warnings`が成功する。
- [OK] `AUT-03` `cargo test`が成功する。2026-09-14時点で60件成功。
- [OK] `AUT-04` `git diff --check`が成功する。
- [OK] `AUT-05` 既定シーケンスが`Original → Notes → Mix → Original`と循環する。
- [OK] `AUT-06` テスト用の`Original → Original → Notes → Notes → Mix → Mix`でも、フェーズ間の固定遷移に依存せず順番に進む。
- [OK] `AUT-07` 音源照合不一致を警告として返し、プロジェクトを読み込める。
- [OK] `AUT-08` 壊れたJSON、未対応形式、無効なプロジェクト値を読み込みエラーとして返す。
- [OK] `AUT-09` sidecar保存に失敗しても、既存sidecarを上書きしない。

## 2. 比較ループ

### 2.1 基本動作

- [OK] `CMP-01` 音源未読込、Loop OFF、または有効なループ範囲がない場合、比較をONにできない。
- [OK] `CMP-02` 有効なループ範囲で比較をONにすると、比較位置がOriginalへ初期化され、再生位置がループ始端へ移動する。停止中にONにした場合は自動再生しない。
- [OK] `CMP-03` ループ1周ごとに`Original → Notes → Mix → Original`と切り替わり、表示中の比較フェーズと実際の出力が一致する。
- [OK] `CMP-04` Originalでは原曲だけ、Notesでは音高メモだけ、Mixでは原曲と音高メモの両方が聞こえる。
- [OK] `CMP-05` 比較をOFFにすると、原曲Muteと各レイヤーのMute・音量が比較開始前から書き換わらず、そのまま有効になる。
- [OK] `CMP-06` 比較中もスペクトログラム上の確認音がOriginal、Notes、Mixのすべてで聞こえる。
- [OK] `CMP-07` 原曲音量0%ではOriginalとMixの原曲が無音になり、音量を戻すと現在フェーズへ即時反映される。

### 2.2 レイヤーミキサー

- [OK] `MIX-01` 比較中もレイヤーミキサーを操作でき、個別Mute・個別音量がNotesとMixへ反映される。
- [OK] `MIX-02` レイヤー1だけをUnmuteした場合、NotesとMixではレイヤー1のメモだけが聞こえる。
- [OK] `MIX-03` 全レイヤーMuteではNotesが無音になり、Mixでは原曲だけが聞こえる。
- [OK] `MIX-04` 全レイヤー音量0%ではNotesが無音になり、Mixでは原曲だけが聞こえる。
- [OK] `MIX-05` Original中に個別Mute・個別音量・全レイヤーMute・全レイヤー音量を変更すると、次のNotesとMixへ変更後の値が反映される。
- [OK] `MIX-06` Originalによる一時全Muteが、レイヤーのMute・音量設定値やプロジェクトのdirty状態を変更しない。
- [OK] `MIX-07` レイヤーの表示OFFや透明度変更は、従来どおり音高メモの発音条件には影響しない。

### 2.3 Transportとループ操作

- [OK] `TRN-01` Loop ON・比較ONのままStopすると比較ONを維持し、再度PlayするとOriginalから正常に再開する。
- [OK] `TRN-02` Pauseでは比較フェーズと再生位置を維持し、再度Playすると同じ位置・フェーズから続行する。
- [OK] `TRN-03` LoopをOFFにすると比較も無条件にOFFになる。
- [OK] `TRN-04` 比較中にトップバーの`|<`を実行すると、ループ始端へ戻り、比較順がOriginalへ戻る。
- [OK] `TRN-05` 比較中に既存ループ範囲の始端・終端を変更すると、変更後の範囲でOriginalから比較を続行する。
- [OK] `TRN-06` 比較中に既存範囲以外からループ範囲を作り直すと、長さゼロの間は比較が解除され、確定後は手動で比較を再度ONにできる。
- [OK] `TRN-07` 新しい音源を開くと比較がOFFへ戻り、以前の音源の比較フェーズを引き継がない。
- [OK] `TRN-08` 通常速度と変更可能な各再生速度で、比較順が音声上のループ1周ごとに進む。
- [OK] `TRN-09` Loop横の`消去`でループ範囲を消去すると比較がOFFになり、続けて`|<`を実行すると曲先頭へシークする。Loopのチェック状態は維持される。
- [OK] `TRN-10` ループ再生中にスペクトログラム下部の表示範囲バーをクリックまたはドラッグすると、音声・ループ範囲・比較フェーズを変えずに表示範囲だけを移動できる。
- [OK] `UI-08` 音高ズーム後、スペクトログラム右端の縦バーをクリックまたはドラッグすると、見切れた高音側・低音側を表示できる。ドラッグ中はカーソルがバー外へ出ても移動を維持する。音高メモや音声再生は変更されない。

### 2.4 境界と音声品質

- [OK] `AUD-01` ループ境界でOriginal／Notes／Mixの切替に聞き取れる遅延や前フェーズの音漏れがない。
- [OK] `AUD-02` 比較フェーズの切替によって、許容できないクリックノイズや音量の跳ねが発生しない。
- [OK] `AUD-03` 短いループでも比較順が飛ばず、UI表示と音声が一致する。
- [OK] `AUD-04` 極小ループで異常終了、フリーズ、大音量ノイズが起きない。再生位置線の既知の逸脱は別の低優先度課題として扱う。
- [OK] `AUD-05` 64声付近の音高メモを含む場合も、比較切替が安定し、アプリが停止しない。
- [NG] `AUD-06` Notes再生時に、Original単独再生では発生しないノイズが混じる場合がある。

## 3. ループ再生中の音高メモ編集

- [OK] `EDT-01` 停止中は従来どおり、音高メモの追加・削除・移動・リサイズ・音高変更・Undoができる。
- [OK] `EDT-02` Loop OFFの通常再生中は、音高メモの追加・削除・移動・リサイズ・音高変更・Undoが禁止される。
- [OK] `EDT-03` 有効なLoop再生中は、音高メモの追加・削除・移動・リサイズ・音高変更・Undoができる。
- [OK] `EDT-04` LoopチェックがONでも有効なループ範囲がなければ、再生中の音高メモ編集が禁止される。
- [OK] `EDT-05` 比較ループ中のOriginal、Notes、Mixのすべてで音高メモを編集できる。
- [OK] `EDT-06` 編集結果が次の周回の正しい開始時刻・音高・長さで発音へ反映される。
- [OK] `EDT-07` ダブルクリック追加では2回目のクリック位置を使用し、既定長0.5秒で作成される。
- [OK] `EDT-08` ループ終端からはみ出す新規メモも0.5秒を維持し、ループ境界で自動短縮されない。
- [OK] `EDT-09` 既存メモをループ範囲外へ移動・リサイズしても、自動削除・自動クランプされない。
- [OK] `EDT-10` ループ終端をまたぐメモは終端で切れ、次周回で開始位置が範囲内なら再度発音する。
- [OK] `EDT-11` 完全にループ範囲外のメモは、その周回では発音しない。
- [OK] `EDT-12` 音高メモの右ドラッグ中は時間ズームと音高ズームが発生しない。
- [OK] `EDT-13` 再生中の追加操作で鳴る確認音と、ループ再生中のメモ音が不自然に競合しない。
- [OK] `EDT-14` テキスト入力中の`Ctrl+Z`がアプリ側の音高メモUndoとして誤消費されない。
- [OK] `EDT-15` Original中にメモを編集し、次のNotesフェーズで変更後のメモが正しく発音される。
- [OK] `EDT-16` Notes中に発音済みのメモを編集しても、現在発音中の音や次フェーズに異常な残音・二重発音が生じない。
- [OK] `EDT-17` Mix中に編集しても、原曲再生には影響せず、次回のNotes／Mixに変更が反映される。

## 4. 既存機能の回帰テスト

### 4.1 保存・読込

- [OK] `SAV-01` 音高メモ、レイヤーID・名前、曲固有設定をsidecar JSONへ保存し、再読込後に復元できる。
- [OK] `SAV-02` レイヤーの表示、Mute、音量、透明度がsidecar JSONへ保存されない。
- [OK] `SAV-03` 保存後の再起動・再読込で、全レイヤーが表示ON、Mute OFF、音量100%、透明度100%へ戻る。
- [OK] `SAV-04` 比較ON/OFF、比較フェーズ、原曲Mute、原曲音量がsidecar JSONやプロジェクトdirty状態へ混入しない。
- [OK] `SAV-05` 音源照合不一致、壊れたJSON、未対応形式、無効値、書込失敗時の既存sidecar保護を自動テストで確認する（`AUT-07`〜`AUT-09`）。

### 4.2 複数レイヤーと編集

- [OK] `LAY-01` 6レイヤーをまたいで、入力先切替、作成、選択、移動、表示、Mute、音量変更、再生ができる。
- [OK] `LAY-02` レイヤー名変更と音高メモを保存・再読込できる。
- [OK] `LAY-03` 複数レイヤーをまたぐ編集を、正しいレイヤーとメモに対してUndoできる。
- [OK] `LAY-04` 全表示、全Mute、全音量、全透明度が6レイヤーへ反映される。
- [OK] `LAY-05` 重なったメモの選択、描画順、レイヤー色が従来どおりである。

### 4.3 再生・表示・操作感

- [OK] `REG-01` 原曲のみ、音高メモのみ、Mixを手動Mute操作でも切り替えられる。
- [OK] `REG-02` 原曲Muteを解除すると、Mute前の原曲音量で再生される。
- [OK] `REG-03` 音高メモ再生が原曲のPlay、Pause、Stop、シーク、ループジャンプへ追従する。
- [OK] `REG-04` 試聴音量、原曲音量、レイヤー音量が互いの設定値を変更しない。
- [OK] `REG-05` 高倍率の時間ズーム・音高ズームでも、描画、メモ編集、再生位置表示が破綻しない。
- [OK] `REG-06` ループ始端・終端付近でも、選択範囲、メモ、再生位置線が期待位置に描画される。
- [OK] `REG-07` 高リフレッシュレート環境で、再生位置追従、比較フェーズ表示、操作応答が安定する。
- [OK] `REG-08` 設定ダイアログの折り畳み、リサイズ、スクロールと各コントロールが正常に動作する。

## 5. 実機確認記録

### 2026-09-14

- 確認済み: 比較ループがOriginal → Notes → Mixの順に動作する。
- 確認済み: 比較中もレイヤーのMute・音量設定がそのまま使用される。
- 確認済み: Loop ON・比較ONのままStopし、再度Playしても正常に再開する。
- 確認済み: ループ範囲の消去後、`|<`で曲先頭へシークできる。
- 確認済み: 再生中の時間表示移動と、音高方向の縦スクロールが動作する。

## 6. リリース判定

- [ ] 上記のv0.4.0比較ループとループ再生中編集に、リリースを妨げる不具合がない。
- [ ] 既存機能の回帰テストに、リリースを妨げる不具合がない。
- [OK] 既知の未対応事項を仕様書、変更履歴、または課題管理へ記録した。


## 7. 既知の課題

- [NG] `AUD-06` Notes再生時のノイズ。Original単独再生では再現しない。
- [ ] 極小ループ範囲をドラッグ編集中、再生位置線だけが範囲外へ進むことがある。音声は範囲内でループしており、優先度は低い。
- [ ] 再生ヘッドが表示右端を越える際の自動追従に軽微な違和感がある。優先度は低い。
