# v0.5.0 テスト項目

## 目的

v0.5.0の可変長音高メモ、表示移動、マウス操作設定、メニューバーと操作ガイドを確認するとともに、既存の再生・比較・保存機能の回帰を確認する。

- `[OK]`: 確認済み・問題なし
- `[ ]`: 未確認
- `[NG]`: 確認済み・問題あり
- 実機確認日、OS、デスクトップ環境、音声デバイス、ウィンドウ／全画面の別は、必要に応じて各項目の末尾へ記録する。

## 1. 自動テスト

- [OK] `AUT-01` `cargo fmt --check`が成功する。
- [OK] `AUT-02` `cargo clippy --all-targets -- -D warnings`が成功する。
- [OK] `AUT-03` `cargo test`が成功する。2026-09-15時点で75件成功。
- [OK] `AUT-04` `git diff --check`が成功する。
- [OK] `AUT-05` 可変長メモの左右ドラッグ、最小長、音源末尾クランプを単体テストする。
- [OK] `AUT-06` 表示バーのクリック中心移動と、つまみ内の掴み位置を維持するドラッグを単体テストする。
- [OK] `AUT-07` 表示幅比例の時間移動と、Linuxで横スクロールとして届く`Shift + ホイール`を単体テストする。
- [OK] `AUT-08` 既定プリセット、操作の入替、Linuxでの`Alt + ホイール`無効化を単体テストする。
- [OK] `AUT-09` ユーザー共通設定JSONの保存・読込、`format_version: 1`、無版旧設定の移行、未対応版の拒否を単体テストする。

## 2. 可変長音高メモ

- [OK] `MEM-01` 停止中に既定の`Shift + 左ドラッグ`で、押下位置から解放位置までの長さのメモを作成できる。
- [OK] `MEM-02` 左から右・右から左のどちらへドラッグしても、左側を開始位置とする正の長さのメモになる。
- [OK] `MEM-03` 作成メモの音高が押下位置の音高になり、選択状態になる。
- [OK] `MEM-04` 極短いドラッグと音源末尾付近のドラッグが、既存の最小長・音源末尾クランプ規則に従う。
- [OK] `MEM-05` 可変長メモ作成を1回の`Ctrl+Z`で取り消せる。
- [OK] `MEM-06` 有効なLoop範囲を再生中に可変長メモを作成でき、Loop OFFの通常再生中は作成できない。
- [OK] `MEM-07` 可変長メモの修飾キーを押している間は、シークと試聴音開始が同時に起きない。
- [OK] `MEM-08` 左ダブルクリックによる既定長0.5秒メモの作成が従来どおり動作する。

## 3. 表示範囲の移動

### 3.1 時間・音高表示バー

- [OK] `VIEW-01` 下部の時間表示バーをつまみ外でクリックすると、クリック位置を表示範囲の中心として移動する。
- [OK] `VIEW-02` 下部の時間表示バーでつまみをドラッグすると、つまみ内の掴み位置を維持して移動する。
- [OK] `VIEW-03` 右端の音高表示バーについても、クリック中心移動と掴み位置を維持したドラッグができる。
- [OK] `VIEW-04` 時間・音高表示バーのドラッグ中にカーソルをバー外へ出しても、ボタンを離すまで移動を維持する。
- [OK] `VIEW-05` 表示バー操作で、音声位置、Loop範囲、比較フェーズ、音高メモ、保存済みプロジェクトが変更されない。
- [OK] `VIEW-06` 再生中・比較中も、時間表示バーで表示範囲だけを移動できる。

### 3.2 ホイール

- [OK] `WHL-01` 既定設定で、ホイールが時間ズーム、`Ctrl + ホイール`が音高ズーム、`Shift + ホイール`が時間移動になる。
- [OK] `WHL-02` `Shift + ホイール`1入力で、時間表示の開始位置が現在の表示幅の15%だけ移動する。例: 表示幅20秒では3秒移動する。ホイール上方向は前方（時間の小さい側）、下方向は後方（時間の大きい側）へ移動し、音源の先頭・末尾では範囲外へ進まない。
- [OK] `WHL-03` Linuxで`Shift + ホイール`が横スクロールとして届く環境でも時間移動できる。
- [OK] `WHL-04` ホイール操作で、音声位置、Loop範囲、比較フェーズ、音高メモが変更されない。

## 4. マウス操作設定

- [OK] `CFG-01` `設定 → マウス操作…`から、既定と時間移動重視のプリセットを選択できる。
- [OK] `CFG-02` 既定プリセットが「なし: 時間ズーム、Ctrl: 音高ズーム、Shift: 時間移動」を割り当てる。
- [OK] `CFG-03` 時間移動重視プリセットが「なし: 時間移動、Ctrl: 時間ズーム、Shift: 音高ズーム」を割り当てる。
- [OK] `CFG-04` 個別設定で使用中のホイール修飾キーを選ぶと、2つの操作の割当が入れ替わり、表示が「カスタム」になる。
- [OK] `CFG-05` 可変長メモ設置の修飾キーをShift・Ctrl・無効から選べる。無効時は修飾キー付き左ドラッグでメモを作成しない。
- [OK] `CFG-06` LinuxではAltがホイール操作・可変長メモ設置の選択肢に表示されず、`Alt + ホイール`でアプリ内の表示操作が起きない。
- [OK] `CFG-07` Linuxで`Ctrl + ホイール`を割り当てた操作が正しく発火する。
- [OK] `CFG-08` 設定変更後にアプリを再起動しても、プリセットまたはカスタム設定が復元される。
- [OK] `CFG-09` `~/.config/otomieru-boyer/settings.json`（または`XDG_CONFIG_HOME`配下）に`format_version: 1`を含めて保存され、曲別sidecarには混入しない。
- [OK] `CFG-10` 無版の旧設定がある場合は形式1へ移行し、未対応の形式番号では設定読込エラーを表示してアプリが起動を継続する（`AUT-09`で確認）。

## 5. メニュー、分析ツール、操作ガイド

- [OK] `MEN-01` メニューバーにファイル、編集、再生、表示、分析ツール、設定、ヘルプが表示される。
- [OK] `MEN-02` ファイルメニューから音源を開け、変更済みプロジェクトでは保存と`Ctrl+S`が動作する。
- [OK] `MEN-03` 編集メニューの元に戻すと`Ctrl+Z`が、再生メニューの再生／一時停止・先頭へ戻る・停止が、ツールバー操作と同じ結果になる。
- [OK] `MEN-04` 表示メニューの初期化で、時間・音高のズームと表示位置が初期状態へ戻る。音声位置・Loop範囲・メモは変更されない。
- [OK] `MEN-05` 分析ツールにはEQ、基音強調、音色、チューニング、レイヤーがあり、マウス操作設定を含まない。
- [OK] `MEN-06` 設定メニューは独立したマウス操作設定ウィンドウを開く。
- [OK] `MEN-07` ヘルプ→操作ガイドに、再生・Loop・比較・音高メモ・表示移動・現在のマウス設定が表示される。
- [OK] `MEN-08` マウス操作設定を変更した直後に、操作ガイドのホイール・可変長メモ説明が更新される。
- [OK] `MEN-09` ウィンドウモードと全画面の両方で、再生時間がメニューバー右端に収まり、不要な縦余白ができない。
- [OK] `MEN-10` ウィンドウ幅を狭めても、常用ツールバーが再生系と表示・音量系の行へ折り返され、右端の操作が見切れない。

## 6. 回帰テスト

- [OK] `REG-01` 再生・Pause・Stop・シーク・Loop・比較再生がv0.4.0どおり動作する。
- [OK] `REG-02` 音高メモの追加・削除・移動・リサイズ・音高変更とUndoが、停止中および編集可能なLoop再生中に動作する。
- [OK] `REG-03` 6レイヤーの入力先、表示、Mute、音量、透明度と一括設定が動作する。
- [OK] `REG-04` 曲固有のメモ、レイヤー名、分析設定をsidecarへ保存・再読込できる。
- [OK] `REG-05` 原曲音量、試聴音量、レイヤー音量、Muteが互いの設定値を変更しない。
- [OK] `REG-06` 高倍率の時間・音高ズームでも、描画、メモ編集、再生位置線が破綻しない。

## 7. 実機確認記録

### 2026-09-15

- 確認済み: 可変長音高メモの作成、表示バーのドラッグ、`Shift + ホイール`による時間移動。
- 確認済み: Linuxでの`Ctrl + ホイール`修正と、`Alt + ホイール`のアプリ内操作禁止。
- 確認済み: メニューバー、分析ツールとマウス操作設定の分離、ウィンドウモードのツールバー配置。

## 8. リリース判定

- [OK] v0.5.0の可変長音高メモ、表示移動、マウス操作設定、メニュー・ヘルプにリリースを妨げる不具合がない。
- [OK] 既存機能の回帰テストにリリースを妨げる不具合がない。
- [OK] 自動テストの成功と、既知課題の記録を確認した。

## 9. 既知の課題

- [NG] `AUD-06` Notes再生時に、Original単独再生では発生しないノイズが混じる場合がある。
- [ ] 極小Loop範囲のドラッグ編集中、再生位置線だけが範囲外へ進むことがある。音声は範囲内でループしており、優先度は低い。
- [ ] 再生ヘッドが表示右端を越える際の自動追従に軽微な違和感がある。優先度は低い。
