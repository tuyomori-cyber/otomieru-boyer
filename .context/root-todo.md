# Auto ToDo Root

## テーマ

- `otomieru-boyer` の音声再生とスペクトログラム表示を、再生状態とUI更新を分離して安定・軽量にする

## 目的

- 再生中のロック競合や再生速度異常を解消する
- 再生位置とUI表示を同期しつつ、スペクトログラム全体を毎フレーム更新しない構成にする
- MVPでは必要十分な再生位置表示を軽量に実現する
- 再生中はスペクトログラムを読み取り専用にし、停止中・一時停止中にスペクトログラム上からシークできる構成にする
- 1画面を任意の開始位置から表示し、再生中は1画面分まるごと表示範囲を更新する。停止中は全体シークバーから表示開始位置を任意に変更できる構成にする
- 1画面の表示範囲と、曲全体の再生位置・ループ範囲を別の概念として扱う
- 曲長ベースのページ分割を再生ロジックの基準にせず、固定幅ビューポートとして表示を扱う方向で整理する
- ページ分割UIまで整ったため、実際のSTFT・静止スペクトログラム表示まで実装済み。サーモグラフィー風の配色と `Heat` 調整も実装済み。ループ範囲指定帯とスペクトログラム上の2本の境界線も実機で一致確認済み。`Loop Range` ラベルは削除し、ループスタートとループエンドの線を個別に微調整できるように実装済み。調整結果はループ再生へ即時反映される。ループ範囲作成と端点微調整のUIも帯の中で分離し、スペクトラム側のループ線ドラッグ処理を撤去済み。ループ開始線・終了線をスペクトラム画面からループ範囲帯まで連続して見えるよう描画した後、帯側の端点表示を三角ハンドルへ変更し、帯とスペクトラムをつなぐ補助線も撤去済み。三角ハンドルの向きを `▶    ◀` として範囲を内側から挟む見た目に変更済み。実機で三角ハンドルの掴みやすさを確認する段階まで進んだ

## 前提認識

- `audio::player` はロック競合しない構造へ組み直し済み
- PCMは不変共有、再生状態と位置はAtomic管理へ変更済み
- `cargo check` でビルド成立を確認済み
- 実機で再生が改善し、通常再生速度は正常化している
- UI更新対象は限定する
  - 高頻度更新: 再生中のスペクトログラム上の再生位置を示す縦線
  - 低頻度更新: 1画面分の表示範囲切り替え
  - 必要時のみ更新: Open、Seek、Stop、表示開始位置変更、将来の選択範囲変更
- スペクトログラムは自動スクロールするが、ヌルヌル動かさない
- 再生中は1画面分再生したら次の表示範囲へ切り替える方式とする
- 再生位置の縦線のみヌルヌル動かし、再生中は手動シークを受け付けない
- 停止中・一時停止中は表示開始位置を任意に変更でき、必要なら滑らかに位置更新する
- ツールバーの再生用シークバーは不要とする
- シーク操作はスペクトログラム側へ寄せる
- 内部の再生位置は秒ベースで保持する
- ツールバーの再生用シークバーを外し、スペクトログラム側にページの概念を追加済み
- 再生中はスペクトログラムを読み取り専用として扱い、停止中のみスペクトログラムクリックによるページ内シークを受け付ける構成まで実装済み
- スペクトログラム下部のシークバーは、表示中ページ内の再生位置を示すものではなく、曲全体のページ列を対象とするページシークバーとする
- 例えば1曲が30ページに分割される場合、ページシークバーは30ページ全体を対象とし、ページジャンプに使用する
- 再生位置の縦線は表示中ページ内の位置を示すもので、全体ページシークバーとは同期させない
- 曲長ベースのページ分割を実装済みで、`page_count = ceil(曲の分数 × 6)` をベースに `8`〜`48`ページへ収める
- 実際の1ページ長は `duration_seconds / page_count` から求める
- スペクトログラム表示側も動的なページ情報を利用し、現在ページとページ内の時間範囲を表示する
- 縦方向・横方向のズームはページ分割をフレキシブルにするため、MVPから除外する
- 再生中はページ内の再生位置縦線を動かし、停止中は全体ページシークバーからページを移動する構成を実装済み
- 全体ページシークバーでページ移動した場合は、そのページの先頭秒へシークする導線を実装済み
- 全体ページシークバーとページ内の再生位置縦線は別概念として扱う
- ページ内再生位置の縦線は、全体ページシークバー上の対応位置にある黄色い丸を表示せず、上から下まで通る縦線として表示する
- 曲長ベースのページ分割を表示へ反映し、再生中のページ内縦線更新と停止中の全体ページバーからページ先頭へ移動する導線を実装済み
- `RustFFT` を追加し、STFTの事前計算を実装済み
- `src/analysis/spectrum.rs` に表示用スペクトログラムデータ構造を実装済み
- 音源読み込み時に解析し、スペクトログラムを `Track` へ格納する構成を実装済み
- ページ分割UIへ静止スペクトログラムの描画を接続済み
- スペクトログラム表示は実機で確認済み
- 強弱が分かりにくかったため、サーモグラフィー風の配色へ変更済み
- ツールバーに `Heat` 調整を追加し、`-24 dB`〜`+24 dB` の表示ゲインを変更できるようにした
- `Heat` を上げると弱い成分が見えやすくなり、下げると強い成分を中心に表示できる
- `cargo check` でビルド成立を確認済み
- 表示密度や色の出方にはまだ調整余地がある
- ループ範囲指定帯は現在表示中の1ページを基準に扱うよう修正済み
- ループ範囲指定帯の表示ラベルも、そのページ内の秒範囲を基準に表示するよう修正済み
- ループ範囲指定帯とスペクトログラム上の2本のループ境界線は実機で一致確認済み
- `Loop Range` ラベルは削除済み
- 停止中にループ開始線と終了線を個別にドラッグして微調整できるよう実装済み
- ループ開始・終了線の調整結果は `selection` とループ再生へ即時反映する構成を実装済み
- ループ範囲作成UIと開始・終了端点の微調整UIを帯の中で分離済み
- スペクトラム側のループ線ドラッグ処理を撤去し、ループ範囲更新は帯UI側から行う構成に整理済み
- 帯側のループ端点表示を赤線から三角ハンドルへ変更済み
- 帯とスペクトラムをつなぐ補助線を撤去済み
- 三角ハンドルのドラッグ判定は開始端・終了端に割り当て済み
- 三角ハンドルの向きを `▶    ◀` に変更し、ループ範囲を内側から挟む見た目に調整済み
- `cargo check` でビルド成立を確認済み
- ページまたぎループ時の問題は、曲長ベースのページ分割そのものよりも、`表示単位` と `再生・編集単位` がずれることが本質的な論点として整理された
- ページは再生やループの基準ではなく、表示上の概念として独立させる案が現時点で有力
- 再生位置・ループ範囲・シークは秒ベースで独立して扱い、ページをまたぐループでも再生ロジック側ではページ境界を意識しない構成を検討する
- 表示は固定ページ番号ではなく、`view_start_seconds` と `view_duration_seconds` を中心とした固定幅ビューポートとして扱う方向が明確になった
- 再生中は `playhead_seconds` が現在の表示範囲を超えたときだけ、1画面分まるごと表示範囲を先へ送る
- 停止中は全体シークバーから `view_start_seconds` を任意の位置へ変更し、その位置から1画面を描画する
- 手動スクロールで表示開始位置を変更しても、現在の再生位置は変更しない。表示中の画面に再生位置が存在しない状態も許容する
- 自動追従は、再生位置が現在の表示範囲の右端を超えたときだけ発生し、その時点で表示範囲を1画面分進めて再生位置を追従させる
- 手動スクロール操作は `page_seek` ではなく `view move` として扱い、再生位置変更と表示位置変更を明確に分離する
- `current_page_*` の再生位置依存ロジックは、`view_start_seconds` / `view_duration_seconds` を基準とする表示範囲ロジックへ置き換える方向で整理する
- 1画面の表示秒数と、全体シークバーの具体的な仕様は未決定
- ページを表示専用概念に落とすか、固定時間ページへ寄せるかという従来の論点は、任意開始位置を持つ固定幅ビューポートへ寄せる方向で整理する
- 秒ベース表示開始位置への変更影響を調査済み
- 変更中心は `AppState` と `spectrogram/timeline` UI 層で、再生エンジンへの影響は小さいと整理された
- 現在の `SpectrogramPaging`、`spectrogram_paging()`、`current_page_start_seconds()`、`current_page_end_seconds()`、`current_page_index()` は「再生位置から現在ページを決める」前提に依存しており、`view_start_seconds` / `view_duration_seconds` への置き換え対象となる
- `spectrogram.rs` は `page_start` / `page_end` / `paging` / `current_page_index` を前提にしており、下部バーも「ページ選択」から「表示開始位置指定」へ意味を変更する必要がある
- `timeline.rs` のループ帯は `view_start_seconds` / `view_end_seconds` を参照する方向で整理でき、変更量は比較的小さい
- `app/mod.rs` は再生中の表示範囲更新と停止中の表示開始位置変更を扱う表示制御の中心になる
- `audio/player.rs` は再生位置・ループ範囲がすでに秒ベースのため、今回の変更による影響は小さい
- `playback.position_seconds` を再生ヘッド、`view_start_seconds` を表示開始位置、`view_duration_seconds` を1画面の長さとして明確に分離する方針とする
- 停止中の全体バー操作では、再生位置変更と表示開始位置変更を混同しないようにする
- `current_page_start_seconds()` など再生位置従属のページ計算を一部だけ残さず、表示範囲の基準を一貫してビューポート側へ寄せる
- 推奨変更順は、`AppState` への `view_start_seconds` / `view_duration_seconds` 導入、`spectrogram.rs` / `timeline.rs` の参照切替、全体バーの意味変更、`app/mod.rs` への表示範囲更新追加、最後に `SpectrogramPaging` 関連を退役させる流れとする

## MVP ゴール

- 音声を正常速度で再生できる
- 再生中はスペクトログラムを読み取り専用として扱える
- スペクトログラム上の再生位置を縦線で確認できる
- スペクトログラムは1画面単位で切り替わり、画面内では縦線だけが滑らかに移動する
- 再生中にスペクトログラム全体を滑らかにスクロールさせない
- 停止中・一時停止中はスペクトログラム上からシークできる
- スペクトログラム下部の全体シークバーから、任意の位置を表示開始位置として選択できる
- 全体シークバーと画面内の再生位置の縦線を同一概念として扱わない
- 表示開始位置を任意の秒として扱い、固定幅の1画面を描画できる
- 手動スクロールでは表示開始位置だけを変更し、再生位置は変更しない
- 表示中の画面に現在の再生位置が存在しない状態を許容する
- 再生中は再生位置が表示範囲の右端を超えたときだけ1画面分進むように表示範囲を更新する
- ループ範囲が画面境界をまたいでも、再生・編集処理を表示範囲に依存させない
- MVPでは縦方向・横方向のズームを扱わない
- 音声からSTFTを事前計算し、静止したスペクトログラムを表示できる
- スペクトログラムで音の強弱を視認しやすい配色・表示ゲインを調整できる
- 表示範囲と再生・編集を独立して扱える構成を目指す

## 非ゴール

- スペクトログラム全体のヌルヌルした自動スクロール
- 再生中の手動シーク操作
- ツールバーに常時表示する再生用シークバー
- 初期段階での高度な選択範囲変更
- 不要なUIの毎フレーム更新
- 全体シークバーと再生位置の縦線の完全同期
- 縦方向・横方向のズーム
- ズームに伴うフレキシブルなページ分割
- ページを再生・ループ処理の単位として扱うこと
- 曲長から固定ページ番号を決めることを再生・表示ロジックの中心にすること

## 段階的 ToDo

1. **音声再生基盤**

- [x] `audio::player` をロック競合しない構造へ変更
- [x] PCMを不変共有に変更
- [x] 再生状態と再生位置をAtomic管理へ変更
- [x] `cargo check` でビルド成立を確認
- [x] 実機で通常再生速度が正常化したことを確認
- [ ] 未解消の場合は `CPAL` のサンプル供給ロジックをさらに最小化して切り分ける

1. **スペクトログラム表示**

- [x] スペクトログラム表示を「現在ページ」の概念つきにする
- [x] 曲の分数からページ数を計算する仕組みを実装する
- [x] ページ数を `ceil(曲の分数 × 6)` をベースに算出し、8〜48ページに収める
- [x] 実際のページ長を曲全体の秒数とページ数から算出する
- [ ] 再生位置からページ番号を求める
- [ ] ページが更新されたら自動シークをそのページ分だけ進める
- [ ] ページ境界を超えたら表示ページだけ切り替える
- [x] ページ内では再生位置の縦線だけを動かす
- [x] スペクトログラム全体を毎フレーム移動させない
- [x] 停止中・一時停止中はスペクトログラム上からシークできるようにする
- [x] スペクトログラム下部にページシークバーを追加する
- [x] ページシークバーを曲全体のページ列を対象とするシークバーとして整理する
- [x] ページシークバーの操作で表示ページをジャンプできるようにする
- [x] ページシークバーとページ内の再生位置の縦線を別概念として扱う
- [x] ページシークバーでページ移動したときは、そのページの先頭秒へシークする
- [x] ページ内再生位置の縦線が全体ページシークバーを貫く表示にし、対応位置の黄色い丸を削除する
- [x] `RustFFT` を追加し、STFTの事前計算を実装する
- [x] 表示用スペクトログラムデータ構造を実装する
- [x] 音源読み込み後にSTFTを事前計算し、`Track` へ格納する
- [x] 既存のページ分割UIへ静止スペクトログラム描画を接続する
- [x] 実機でスペクトログラム表示を確認する
- [x] スペクトログラム配色をサーモグラフィー風に変更する
- [x] ツールバーに `Heat` 調整を追加し、`-24 dB`〜`+24 dB` の表示ゲインを変更できるようにする
- [ ] `Heat` の増幅・減衰を実機で比較し、最も見やすい表示条件を確認する
- [ ] `page_count` 中心の表示設計を `view_start_seconds` と `view_duration_seconds` 中心へ寄せる
- [ ] `AppState` に `view_start_seconds` と `view_duration_seconds` を導入する
- [ ] `spectrogram.rs` の `page_start` / `page_end` / `paging` / `current_page_index` 依存を `view_*` 参照へ切り替える
- [ ] 1画面の表示秒数を決める
- [ ] 停止中に任意の `view_start_seconds` から1画面を描画できる構成を整理する
- [ ] 全体バーをページ選択から任意の表示開始位置指定へ変更する
- [ ] 手動スクロール時は `view_start_seconds` のみ変更し、`playback.position_seconds` を変更しない方式へ整理する
- [ ] 再生位置が表示範囲の右端を超えた時だけ、1画面分表示範囲を更新する方式へ整理する
- [ ] `SpectrogramPaging` と関連するページ計算を表示ロジックから退役させる

1. **再生位置UI**

- [x] ツールバーの再生用シークバーを外す
- [x] 再生中はスペクトログラム上の再生位置縦線のみを高頻度更新する
- [x] スペクトログラム上のクリックで再生位置の縦線を移動できるようにする
- [ ] Open、Seek、Stop、表示開始位置変更時は必要なUIのみ更新する
- [x] ページ内縦線と全体ページバーの実機挙動を確認する
- [x] ループ範囲指定帯を現在表示中の1ページ基準で扱う
- [x] ループ範囲指定帯の表示ラベルをページ内秒範囲へ変更する
- [x] 実機でループ範囲指定帯のドラッグ位置とスペクトログラム上のループ境界線が一致することを確認する
- [x] `Loop Range` ラベルを削除する
- [x] ループ開始線と終了線を個別にドラッグして微調整できるようにする
- [x] ループ線の調整結果をループ再生へ即時反映する
- [x] ループ範囲作成UIと開始・終了端点の微調整UIを帯の中で分離する
- [x] スペクトラム側のループ線ドラッグ処理を撤去し、ループ範囲更新を帯UIへ集約する
- [x] ループ開始線・終了線をスペクトラム画面からループ範囲帯まで連続して見えるように描画する
- [x] 帯側のループ端点表示を赤線から三角ハンドルへ変更する
- [x] 帯とスペクトラムをつなぐ補助線を撤去する
- [x] 三角ハンドルの向きを `▶    ◀` に変更し、範囲が分かりやすい見た目に調整する
- [ ] 三角ハンドルの個別ドラッグ操作の実機での触り心地を確認する
- [ ] 必要なら三角ハンドルのサイズやヒット幅を微調整する
- [ ] `timeline.rs` のループ帯を `view_start_seconds` / `view_end_seconds` 基準へ切り替える
- [ ] ページまたぎループ時に、表示範囲をどのように扱うか決める
- [ ] ループ中に現在位置への表示追従とループ範囲全体の可視性のどちらを優先するか決める
- [ ] 表示範囲がループ境界をまたいでも、ループ再生を表示範囲に依存させない構成へ整理する

1. **STFT・静止スペクトログラム**

- [x] `src/analysis/stft.rs` のSTFT処理を実装する
- [x] `src/analysis/spectrum.rs` の表示用スペクトログラムデータ構造を実装する
- [x] 音声読み込み後にSTFTを事前計算する
- [x] まず1ページ分の静止スペクトログラムを実際に描画する
- [x] 既存のページ分割UIと静止スペクトログラムを接続する
- [ ] ピアノ鍵盤との縦軸一致へ進む

## 未解決論点

- `page_count` 中心の表示設計から `view_start_seconds` / `view_duration_seconds` 中心へ移行する具体的な実装
- `AppState` に表示開始位置と表示幅をどのように保持するか
- 1画面の表示秒数をどのように決めるか
- 停止中の全体シークバーを、任意の `view_start_seconds` へ移動する操作としてどう実装するか
- 手動スクロール時に再生位置を変更せず、表示開始位置だけを変更する具体的な実装
- 再生中に再生位置が表示範囲の右端を超えた時だけ表示範囲を1画面分切り替える具体的な実装
- 現在のページ番号ベースの処理をどこまで残し、どこをビューポートベースへ置き換えるか
- 曲長からページ数を求める従来の計算式をどこまで廃止するか
- `spectrogram.rs` と `timeline.rs` のページ依存をどの順序で `view_*` へ切り替えるか
- `app/mod.rs` で再生ヘッドと表示範囲をどのように分離して更新するか
- サーモグラフィー風スペクトログラムの `Heat` 値をどこに設定すると強弱が最も見やすいか
- 実機でのスペクトログラム表示密度や色の出方の確認・調整
- 静止スペクトログラムとピアノ鍵盤の縦軸を一致させる具体的な実装
- スペクトログラム上のクリック音確認
- 三角ハンドルの掴みやすさと個別ドラッグ操作の触り心地
- 三角ハンドルのサイズやヒット幅を調整する必要があるか
- ページまたぎループ時に表示範囲を自動切り替えするか
- ループ中は現在位置追従よりループ範囲全体の可視性を優先するか
- 表示範囲とループ範囲が一致しない場合でも、再生位置・ループ範囲・シークを秒ベースで独立して扱う具体的な構成

## 次の一手

1. `AppState` に `view_start_seconds` / `view_duration_seconds` を導入し、`current_page_*` の再生位置依存ロジックを `view_*` 基準へ置き換える設計を固める
2. 全体バーを「ページ選択」ではなく「表示開始位置変更」として扱い、手動操作では再生位置を変更せず、再生位置が表示右端を超えた時だけ1画面分追従する方式を具体化する
3. `timeline.rs` / `spectrogram.rs` の表示範囲参照を `view_*` へ切り替え、ページ依存処理を退役させる順序を整理する