編集

# Auto ToDo Root

> このファイルは `llmctx` が `Stop` hook ごとに更新する作業用 ToDo です。
>
> 本番 ToDo への最終マージは手動で行ってください。

## テーマ

- `仕様書.md` を実装可能な段階計画へ落とし込み、耳コピ向け Linux ネイティブ MVP を Rust で組み上げる
- 音響解析ツール化を避けつつ、「開く・聴く・遅くする・繰り返す・見る・クリックして確かめる」を最短で成立させる

## 目的

- MVP 完成条件に直結する機能を、仕様書の推奨開発順序に沿って順番に実装できる状態を維持する
- 再生系、解析系、描画系の責務を分離し、再生位置とスペクトログラムが常に同期する基盤を作る

## 前提認識

- 対象アプリは Linux ネイティブの耳コピ支援プレイヤーであり、総合音響解析ソフトは目指さない
- ダウンロード、MuseScore 直接連携、自動採譜、AI 解析、詳細 FFT 調整は MVP 外
- 速度変更より先に、通常再生・スペクトログラム表示・同期スクロールを完成させる
- スペクトログラムは事前計算方式とし、再生中に FFT を回し続けない
- スペクトログラムの時間軸は元音源基準で保持し、速度変更時も解析データは再生成しない
- UI には解析パラメータを出さず、ユーザーには音階ベースの見え方だけを提示する
- リポジトリは既存実装なしで、`仕様書.md` のみがある新規着手状態であることを確認済み
- 実装は既存コードの差分修正ではなく、初期セットアップから開始する
- 初期セットアップ後は `eframe/egui` の最小アプリを起点に、仕様書準拠の `src/` 骨格を作成する
- Rust プロジェクトの初期セットアップと `eframe/egui` の最小ウィンドウ起動まで完了している
- `app` / `audio` / `analysis` / `ui` / `model` の仕様書準拠の骨格まで作成済みである
- `cargo check` と `cargo fmt` により初期構成の成立を確認済み
- 次段階は `Symphonia` を追加して `Open` と音声デコードを実装し、`Track` 生成へ進む

## MVP 完成条件

- MP3 などの音声ファイルを開ける
- 再生、一時停止、停止、シーク、再生位置表示ができる
- スペクトログラムがピアノ鍵盤と縦軸同期して表示される
- 再生位置カーソルと表示スクロールが同期する
- スペクトログラム上のドラッグで範囲選択できる
- 選択範囲をループ再生できる
- スペクトログラム上のクリックで対応音程の基準音を鳴らせる
- `0.50x` または `0.75x` でもピッチを維持した再生ができる

## 非ゴール

- YouTube ダウンロード機能の統合
- MuseScore の直接操作や MIDI 送信
- 自動採譜、コード認識、BPM 解析、楽器分離、ボーカル除去
- 波形編集、音声編集、DAW 機能、VST、プラグインシステム
- 複数トラック対応や詳細解析設定の露出

## 推奨内部構成

- `app/`: アプリ状態と全体統合
- `audio/decoder.rs`: 音声ファイル読み込みと PCM 化
- `audio/player.rs`: 再生、一時停止、停止、シーク、現在位置、ループ境界処理
- `audio/timestretch.rs`: ピッチ維持速度変更の境界
- `audio/preview_tone.rs`: クリック音程確認用の基準音再生
- `analysis/stft.rs`: Windowing、FFT、Magnitude 計算
- `analysis/spectrum.rs`: 表示用スペクトログラムデータ構造
- `analysis/pitch_map.rs`: `frequency ↔ MIDI` と `screen Y ↔ pitch`
- `ui/spectrogram.rs`: 中心描画、再生位置、クリック、ドラッグ選択、スクロール
- `ui/piano.rs`: 鍵盤描画と縦軸同期
- `ui/toolbar.rs`: Open、Play/Pause、Speed、Time、Loop
- `model/`: `Track`、`PlaybackState`、`Selection` のような中核データ

## マイルストーン

- M1: `eframe/egui` で空ウィンドウを起動できる
- M2: 音声ファイルを読み込み、PCM と基本メタデータを保持できる
- M3: 通常速度で再生、一時停止、停止、シークができる
- M4: STFT を事前計算し、静止スペクトログラムを描画できる
- M5: ピアノ鍵盤とスペクトログラムの縦軸が一致する
- M6: 再生位置カーソルと同期スクロールが動く
- M7: クリック音程確認、範囲選択、ループ再生が使える
- M8: `Rubber Band` によるピッチ維持速度変更を統合する
- M9: 実際の耳コピ作業で使って調整する

## 段階的 ToDo

- [x] `仕様書.md` を読んで MVP の要求、非ゴール、開発順を整理する
- [x] `仕様書.md` の内容を `.context/root-todo.md` に反映する
- [x] リポジトリの現状実装を確認し、既存実装がなく `仕様書.md` のみであることを確認する
- [x] `Cargo.toml` と crate 構成を決め、最小アプリを起動する
- [x] `src/` 配下の責務分割を仕様書どおりの骨格へ寄せる
- [ ] `Symphonia` を追加し、`audio::decoder` で音声ファイル読み込みと PCM 化を実装する
- [ ] `audio::decoder` の WAV/MP3/FLAC/OGG の初期対応範囲を確認する
- [ ] `Track` 相当モデルに PCM、サンプルレート、長さ、解析結果の参照を保持できるようにする
- [ ] `Open` からデコード済み音声を `Track` として生成できるようにする
- [ ] `audio::player` で通常速度の再生、一時停止、停止、シーク、現在位置管理を実装する
- [ ] UI thread / audio callback / worker の分離方針をコードへ落とす
- [ ] 読み込み後に worker で STFT を事前計算する
- [ ] 表示用スペクトログラムキャッシュを作る
- [ ] `analysis::pitch_map` で MIDI ベースの縦軸変換を実装する
- [ ] `ui::spectrogram` で静止表示を描く
- [ ] `ui::piano` で鍵盤表示と縦位置同期を実装する
- [ ] 再生位置カーソルを表示する
- [ ] カーソル固定方式の同期スクロールを実装する
- [ ] スペクトログラムクリックで音名推定と基準音再生を行う
- [ ] 横ドラッグで時間範囲を選択できるようにする
- [ ] 選択範囲ループ再生を実装する
- [ ] `audio::timestretch` の抽象境界を先に定義する
- [ ] `Rubber Band` を接続し、`0.50x` / `0.75x` / `1.00x` を提供する
- [ ] ツールバーと時刻表示を整理し、「見ただけで使い方が分かる」UI に寄せる
- [ ] 実運用で耳コピし、UX 上の不足を洗い出す

## 設計上の注意

- `audio::player` は UI を知らず、元音源時間ベースの `position` 管理に集中させる
- UI 側は再生状態を個別保持せず、中核 state を参照して描画と入力反映だけを行う
- 音声コールバック内で重い処理をしない
- 解析中に GUI を固めない
- 速度変更時は position の進み方だけを変え、スペクトログラム座標は変えない
- クリック音確認は初期実装では正弦波でよい

## 未解決論点

- `Symphonia` を使ったデコーダ実装と、WAV/MP3/FLAC/OGG の初期対応範囲が未確定
- `Track` 生成時に PCM、サンプルレート、長さ、解析結果参照をどう保持するか未実装
- 通常再生に使用する音声バックエンドと `audio::player` の具体的な実装方式が未確定
- `Rubber Band` の Rust 側接続方式と導入コストが未確定
- スペクトログラム表示のキャッシュ形式と描画負荷対策が未確定
- ループ境界とシーク精度をどの単位で管理するか未確定

## 次の一手

1. `Symphonia` を追加し、`Open` から音声デコードと `Track` 生成までを実装する
2. `audio::player` に通常速度の再生基盤を入れ、再生・一時停止・停止・シーク・現在位置管理へ進む
3. デコーダとプレイヤーの責務を分離した状態で、STFT の事前計算へ進める構成を確認する