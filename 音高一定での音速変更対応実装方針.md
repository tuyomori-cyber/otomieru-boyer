実装方針は「音源全体を加工する方式」を廃止し、workerが少量ずつ生成して固定長リングバッファへ渡すストリーミング方式にします。
```
UI（停止中のみ設定変更）
        │ Prepare / Seek / Stop
        ▼
TimeStretch worker
  元PCM → time stretch → 必要ならリサンプル
        │
        ▼
固定長SPSCリングバッファ
        │
        ▼
CPAL audio callback
  取り出して出力するだけ
```
## 設計原則
- UIスレッドではDSP計算をしない
- 音声コールバックでは、メモリ確保・Mutex・DSP計算をしない
- 加工済み音源全体を保持しない
- DSP追加メモリは音源長に関係なく固定
- 現在安定している通常再生経路は、音程維持OFF時にそのまま残す
- 速度・音程設定は従来どおり停止中のみ変更可能
- シーク、ループ、停止時は古い生成結果を破棄できるようにする
## コンポーネント
### TimeStretchBackend
アルゴリズムを再生制御から分離します。
```
trait TimeStretchBackend {
    fn configure(&mut self, sample_rate: u32, channels: usize, speed: f32);
    fn reset(&mut self, source_frame: u64);
    fn process(&mut self, input: &[f32], output: &mut [f32]) -> ProcessResult;
}
```
本番アルゴリズムは、ストリーミング対応の実績あるライブラリを第一候補にします。プロジェクト方針にある Rubber Band を優先し、ライセンス・Linux配布方法を確認して採否を決めます。
独自WSOLAを採用する場合も、この境界の内側だけで実装します。その場合は以下を必須にします。
- Hann窓を初期化時に一度だけ計算
- 固定サイズの作業領域を再利用
- process() 中に Vec を生成しない
- 音源長に比例した配列を作らない
### TimeStretchWorker
専用スレッドで次を担当します。
- DSP設定の適用
- 元PCMから必要な範囲だけ読み出す
- time stretchをブロック単位で実行
- リングバッファが満杯なら休止
- 残量が減ったら生成を再開
- シークや停止時に処理世代を切り替える
各要求には generation_id を付けます。シークや設定変更後に古い処理結果が完成しても、世代が違えば破棄します。
### 固定長リングバッファ
0.5～1秒分程度を上限にします。
48kHz・ステレオ・f32なら、1秒でも約384KBです。DSP内部領域を含めても、追加メモリは数MiB以内を目標にします。
音声コールバックはリングバッファから読み出すだけにします。残量不足時はブロックせず無音を出し、underrun回数を記録します。
### 再生準備状態
設定変更後、UIを止めずにworkerで先読みします。
```
Idle → Preparing → Ready → Playing
            │
            └── Error
```
- Preparing: 「再生準備中」を表示
- Ready: 100～200ms程度の先読み完了
- Playing: Playを許可
- Error: 通常再生へ安全に戻せる状態
### 元音源時間の維持
UI、シーク、ループは従来どおり元音源フレームを基準にします。
出力1フレームごとに、論理的な元音源位置を次の量だけ進めます。
```
source_step = speed_ratio × source_sample_rate / output_sample_rate
```
加工バッファの位置を逆数で参照する方式は廃止します。worker自身が「現在の元音源位置から、次に必要な出力音」を生成します。
Transportイベントは次のように扱います。
- Start: 先読み済みバッファから再生
- Pause: バッファとDSP状態を維持
- Stop: バッファを破棄し、曲頭から再準備
- Seek: 世代更新、バッファ破棄、指定位置から再準備
- LoopJump: ループ先頭からDSPを再同期して先読み
## 段階的な実装順
1. 計測基盤を先に追加
   - worker処理時間
   - リングバッファ残量
   - underrun回数
   - DSP確保メモリ
   - prepare完了時間
2. パススルーworkerを実装
   まだ音程維持はせず、元PCMをworkerからリングバッファへ流します。ここでUI、コールバック、シーク、停止が安定することを確認します。
3. time stretch backendを接続
   最初は 1.00x だけを通し、通常再生と同じ音になることを確認します。その後、0.75x、0.50x、1.25x、1.50xを順番に有効化します。
4. シーク・ループを接続
   世代管理、バッファ破棄、再先読みを実装します。ループ境界のクリックノイズも確認します。
5. 古い全体バッファ方式を削除
   [dsp_engine.rs (line 99)](/home/tuyomori/project/otomieru-boyer/src/audio/dsp_engine.rs:99) の build_playback_samples() 呼び出しと playback_samples 全体保持を廃止します。[timestretch.rs (line 49)](/home/tuyomori/project/otomieru-boyer/src/audio/timestretch.rs:49) の全PCMコピーも削除対象です。
6. 音質調整
   音程維持、トランジェント、ステレオ像、ループ境界を実機で調整します。独立したpitch shiftは、この経路が安定した後に追加します。
### 合格条件
- 30分音源でもDSP追加メモリが32MiB以内
- 停止中の設定変更でUIが100ms以上固まらない
- 10分再生でメモリが単調増加しない
- 先読み完了後のunderrunが0回
- 音声コールバック内のヒープ確保が0回
- 0.50x / 0.75x / 1.25x / 1.50xで基準音の音高差が±10 cent以内
- シーク・ループ後に古い音声が混入しない
- 音程維持OFFでは現在の安定した通常再生が維持される
最初の実装単位は「パススルーworker＋固定長リングバッファ」です。ここでCPU・メモリ・UI安定性を確認してから、time stretchアルゴリズムを接続するのが安全です。