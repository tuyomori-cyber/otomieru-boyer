# 変更履歴

この文書は、ユーザーに見える機能・操作・保存形式の変更を記録する。詳細な現行仕様は[全体仕様書](<specification/仕様書.md>)、音高メモの詳細は[v0.3.0 音高メモ仕様書](<specification/v0.3.0 音高メモ仕様書.md>)を参照する。

## v0.7.0

### 追加

- ツールバーとメニューバーから開く、常用ツール用の`ツール`ダイアログを追加
- `ツール → マスターパレット…`で、表示するツールの選択、機能名のドラッグによる並べ替え、既定構成への復帰を追加
- `Original`、`Notes`、`Mix`を任意順・任意回数で最大10件まで並べる比較パターンを追加

### 変更

- 旧「分析ツール」を、耳コピ中に使う機能をまとめる「ツール」へ改称
- 比較ループを固定の`Original → Notes → Mix`から、変更可能な比較パターンへ拡張。初期パターンは従来どおり

### 保存形式

- ユーザー共通の`settings.json`に、ツールパレットの表示対象・マスター順と比較パターンを保存。曲別sidecarには保存しない
- 旧`settings.json`は既定の比較パターンを適用し、既存の表示順をマスター順の先頭として引き継ぐ

## v0.6.0

### 追加

- 同一音源を再読込する際、STFT・スペクトログラム解析を再利用するバイナリキャッシュを追加

### 保存形式

- 音源横に`<音源ファイル名>.otomieru.spectrum.bin`を作成。プロジェクトsidecar JSONとは分離し、音源照合情報と解析設定が一致しない場合は自動再生成

### 対象外

- 再生用音声のデコードキャッシュ、初回解析の非同期化、進捗表示、キャンセル

## v0.5.0

### 追加

- 修飾キー（既定: Shift）+ 左ドラッグによる可変長音高メモ設置を追加
- 時間・音高表示バーのクリック中心移動と、つまみ内の掴み位置を維持するドラッグを追加
- Shift + ホイールによる時間表示移動を追加
- マウス操作プリセットと、ホイール3操作・可変長メモ設置の個別設定を追加
- ファイル、編集、再生、表示、分析ツール、設定、ヘルプのメニューバーと操作ガイドを追加

### 変更

- EQ、基音強調、音色、チューニング、レイヤーを「分析ツール」へまとめ、マウス操作設定を独立した「設定」画面へ分離
- Open、Save、分析ツールをメニューバーへ移動し、耳コピ中の常用操作はツールバーに維持
- ウィンドウ幅に応じて再生系と表示・音量系を2行に分けるツールバーへ変更

### 保存形式

- ユーザー共通の`settings.json`を追加。`format_version: 1`でマウス操作設定を保存し、曲別sidecarには保存しない

### 既知の課題・対象外

- タッチパッドの連続スクロールジェスチャーに対する時間移動量の調整は未対応

## v0.4.0

### 追加

- 有効なループ再生中の音高メモ編集とUndoを追加
- `Original → Notes → Mix`を各1周ずつ再生する比較ループを追加
- Loop横にループ範囲の`消去`ボタンを追加
- 再生中にも操作できる時間方向の表示範囲バーを追加
- 音高ズーム後の表示範囲を移動する縦バーを追加。ドラッグ中はバー外へ出ても操作を継続

### 変更

- テキスト入力中の`Ctrl+Z`はメモUndoではなく入力欄のUndoを優先
- 比較中のNotes／Mixは、現在のレイヤーMute・音量設定をそのまま使用
- Originalではレイヤー設定値を変更せず、メモ音だけを一時的に無音化

### 既知の課題

- Notes再生時にノイズが混じる場合がある
- 極小ループ範囲をドラッグ編集中、再生位置線だけが範囲外へ進む場合がある
- 再生ヘッドが表示右端を越えた時の自動追従には軽微な違和感がある

## v0.3.0 — 2026-09-13

### 追加

- 音源ごとのプロジェクトデータとsidecar JSON（`<音源ファイル名>.otomieru.json`）を追加
- 音高メモの作成、削除、端部リサイズ、右ドラッグによる時間・音高移動、`Ctrl+Z`を追加
- 原曲と同期して音高メモを鳴らす答え合わせ再生を追加（最大64声）
- 固定6レイヤーの入力先切替、名前変更、固定色による同時表示を追加
- レイヤーごとの表示、Mute、音量、透明度、および全レイヤー一括操作を追加
- ツールバーに原曲音量を追加し、確認音・音高メモ再生の音量から分離
- 設定・調整ダイアログにレイヤー設定を追加

### 変更

- 設定・調整ダイアログを`EQ → 基音強調 → 音色 → チューニング → レイヤー`の縦順へ変更
- 設定項目を折り畳み式にし、説明をⓘのホバー／クリック表示へ移動
- 設定・調整ダイアログをリサイズ・スクロール対応へ変更
- 試聴音量を、スペクトログラムを押している間の確認音専用に変更
- レイヤーの表示、Mute、音量、透明度を保存対象から除外。起動・sidecar読込時に、表示ON、Mute OFF、音量100%、透明度100%へ初期化
- 旧sidecarに上記レイヤー実行時設定が含まれていても読み込めるが、値は使用せず初期値を適用

### 保存形式

- `format_version: 1`を導入
- 保存対象は音源照合情報、曲固有設定（A4基準Hz・EQ・基音強調）、レイヤーID・名前、音高メモ
- 保存対象外はレイヤー実行時設定、選択状態、Undo履歴、再生速度、オクターブ、ループ、各音量、音色

## v0.2.0 — 2026-09-11

### 追加

- スペクトログラム押下中に対応する音を鳴らす確認音を追加
- ピアノ、シンセストリングス、矩形波の確認音色を追加
- A4基準ピッチの調整と確認音量の調整を追加
- 基音強調を追加。キー／スケールまたは12音の個別指定を使い、表示上の基音候補を補助
- スペクトログラムとピアノロールのスクリーンショット、同梱のFreePats音源とライセンス情報を追加

### 変更

- EQ、基音強調、音色の調整UIを`調整・設定`ダイアログへ統合
- スペクトログラム押下時に、確認音と対応するピアノ鍵を連動して着色
- 再生位置の更新とスペクトログラム描画を改善し、縮小表示時の描画量を表示幅に応じて抑制

## v0.1.0 — 2026-08-21

### 初回リリース

- WAV / MP3 / FLAC / OGG / M4A / AAC の読み込みと再生
- 再生、一時停止、停止、曲頭へ移動、スペクトログラム上でのシーク
- タイムラインによる区間ループ
- 音程を維持する速度変更と、独立したオクターブシフト
- MIDI 24〜108を対象とするスペクトログラムとピアノロール
- 時間軸・音高方向のズーム、表示ゲイン（Heat）
- 5バンドEQと、再生音・スペクトログラム表示への反映
- Rubber BandとSPSCリングバッファを使うストリーミング再生基盤
